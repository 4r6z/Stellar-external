// OLD DRIVER - DISABLED
// This driver has been replaced with a new implementation
// The old nevermissbetter driver is no longer functional

#include "driver.hpp"

// These variables are disabled and no longer used
// uintptr_t virtualaddy = 0;
// HANDLE memory::driver_handle = nullptr;
// INT32 memory::process_id = 0;